# Mirror of the conda-forge staged-recipes submission (PR #33928).
# Feedstock will be created after review; prefer the feedstock recipe thereafter.
schema_version: 1

context:
  version: "0.11.2"

package:
  name: snapper-fmt
  version: ${{ version }}

source:
  url: https://github.com/TurtleTech-ehf/snapper/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 3b6fa02fd70e3d25b77b0f40c324cd0d3312a315b56d2371b655569d6ef61467

build:
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses

tests:
  - script:
      - snapper --version
      - snapper --help
      - snapper-fmt --version
      - snapper-fmt --help
  - package_contents:
      bin:
        - snapper
        - snapper-fmt

about:
  homepage: https://snapper.turtletech.us
  summary: Semantic line break formatter for Org, LaTeX, Markdown, and plaintext
  description: |
    Snapper reformats prose so each sentence sits on its own line (semantic
    line breaks), preserving markup structure for Org, LaTeX, Markdown, RST,
    and plaintext. It ships a CLI, optional neural sentence splitting, an LSP
    server, file watching, and a pandoc AST backend.
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  documentation: https://snapper.turtletech.us
  repository: https://github.com/TurtleTech-ehf/snapper

extra:
  recipe-maintainers:
    - HaoZeke
